# Secrets

Put the Google service-account JSON key here as:

    google-service-account.json

It is a credential. It is gitignored and blocked from the web by .htaccess in
this folder, and it must never be committed. Upload it over Hostinger's File
Manager or SFTP, not through Git.

See docs/google-calendar-setup.md for how to generate it.
