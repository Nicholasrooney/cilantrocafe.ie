# PHP in here is included by the pages, never requested directly.
# Blocking direct access keeps config, secrets and internals off the web.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
